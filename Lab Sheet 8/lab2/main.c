// 2.	Write a function which calculates and displays the sum of 2 equally long array (size 100).

#include <stdio.h>

int main()
{

 int n, ww, usrarr2, usrarr[100];

 printf("Enter data to store inside first array: ");
 scanf("%d", &n);

 printf("Enter data to store inside second array: ");
 for (ww = 0; ww < n; ww++)
 {
  scanf("%d", &usrarr[usrarr2]);
 }

 // Calculate the sum
 for (ww = 0; ww < n; ww++)
 {
  // Sum method goes here.
 }
}